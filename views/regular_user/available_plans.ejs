<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Workout plans</title>
    <style>
      .meal-plan,
      .workout-plan {
        background-color: #f0f4f8;
        border: 1px solid #d1d9e6;
        border-radius: 8px;
        padding: 20px;
        margin: 10px 0;
      }
    </style>
  </head>
  <body>
    <h1>
      Reach your fitness goals by choosing a meal and workout plan according to
      your needs
    </h1>
    <%- include("../_header.ejs") %>
    <section>
      <div>
        <h2>Meal Plans</h2>
        <% meals.forEach(meal =>{%>
        <div class="meal-plan">
          <h3><%= meal.title %></h3>
          <p><%= meal.description %></p>
          <p>Target: <%= meal.target_goal%> <%= meal.calories %> Calories</p>
        </div>

        <%})%>
      </div>
    </section>
    <section>
      <div>
        <h2>Workout Plans</h2>
        <% workouts.forEach(workout=>{%>
        <div class="workout-plan">
          <h3><%= workout.title %></h3>
          <p><%= workout.description %></p>
          <p>Level: <%= workout.difficulty %></p>
          <p>Duration:<%= workout.duration_minutes %> Minutes</p>
          <p>Target:<%= workout.target_goal%></p>
        </div>

        <%})%>
      </div>
    </section>
  </body>
</html>
